A really fun really basic one. The idea here is simple. Take the classic Alice, Bob and Eve example from cryptography, but where each of them is a network and the entire thing is trained via GAN. Done.
